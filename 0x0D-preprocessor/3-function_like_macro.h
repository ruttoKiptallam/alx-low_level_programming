#ifndef FUNTIONS_LIKE_MACRO_H
#define FUNTIONS_LIKE_MACRO_H

/*
 *writting a function-like macro ABS(x) that computes the absolute value of a number x
 *
 *
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))
#endif
